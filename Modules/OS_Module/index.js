// os module includes the utility properties related to operating system 
const os = require('os');
 console.log(os.freemem() );
 console.log(os.version());
 console.log(os.arch());
 console.log(os.release());
 console.log(os.tmpdir());
 console.log(os.hostname());
 console.log(os.machine());
 console.log(os.homedir());

  // this module includes the all the functions related to the os 